
//walk over all targets, return the one which has cid or name match
//if more than 1 found, then is error
fn  cid_name_find[T](targets []T, cid_name string) !T {
	// URGENT: implement cid_name find
}
